import { Container, Typography } from '@mui/material';
import React from 'react';

const HomePage: React.FC = () => {
  return <Container maxWidth='sm'>
    <Typography component='h2'>Let's do some shit!!!</Typography>
  </Container>;
};

export default HomePage;